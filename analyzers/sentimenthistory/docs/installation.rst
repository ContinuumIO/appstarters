============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install sentimenthistory
    $ pip install sentimenthistory

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sentimenthistory
    $ pip install sentimenthistory
